/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package gradle.multimodule.java.app;

import gradle.multimodule.java.list.LinkedList;

import static gradle.multimodule.java.utilities.StringUtils.join;
import static gradle.multimodule.java.utilities.StringUtils.split;
import static gradle.multimodule.java.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
